package pl.brokenranks.tool.broken_ranks_tool.optimization.dto;

/** Selects the source of drifs and whether the result should be treated as a recommendation. */
public enum OptimizationMode {
    BUILD_FROM_SCRATCH,
    ADVISOR
}
